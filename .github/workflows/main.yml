name: Build Kernel A35 KSU
on:
  workflow_dispatch: # هذا يضيف زر التشغيل اليدوي
  push:
    branches:
      - main # يبدأ البناء تلقائياً عند حفظ الكود

jobs:
  build_kernel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison libssl-dev libelf-dev clang llvm lld curl wget git zip

      - name: Build Start
        run: |
          echo "الآن يمكنك إضافة أوامر تحميل السورس والبناء هنا"
          # سأترك لك مكاناً لوضع رابط السورس الخاص بك
